# SQLite环境配置（保留用于兼容）
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:./data/personal-ledger.db
    username: 
    password:

mybatis-plus:
  global-config:
    db-config:
      id-type: none
  configuration:
    use-generated-keys: false
